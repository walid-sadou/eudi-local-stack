# URL publique HTTPS donnée par cloudflared (celle qui apparaîtra dans le QR)
PUBLIC_URL=https://circumstances-leg-parliamentary-donors.trycloudflare.com

# Origine autorisée côté CORS (identique à PUBLIC_URL)
UI_ORIGIN=https://circumstances-leg-parliamentary-donors.trycloudflare.com
# Comment l’UI atteint le backend à l’intérieur du réseau compose
HOST_API=http://verifier:8080

